# Beforegram

A dependency-free, local-first Instagram pre-launch simulator.

Open `dist/index.html` directly or serve the `dist` directory with any static host.

Account data and uploaded images are stored only in the browser's localStorage.
